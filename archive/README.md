# Archive

Historical prototypes kept out of the main product path.

| Path | Notes |
|------|--------|
| `streamlit_app.py` | Early Streamlit UI. Superseded by `web/` + `api/`. |

To run the archived UI (needs `streamlit` / `plotly` installed):

```bash
.venv\Scripts\pip install streamlit plotly
.venv\Scripts\streamlit run archive/streamlit_app.py
```

**Product path:** FastAPI + Next.js — see root `README.md`.
